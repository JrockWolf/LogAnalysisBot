# Example environment variables
# If you have an OpenAI API key, set it here. Example: sk-...
OPENAI_API_KEY=
# If you have a Perplexity.ai key, set it here instead (keys often start with 'pplx-').
# If you mistakenly put a Perplexity key into OPENAI_API_KEY, the adapter will avoid calling OpenAI.
PERPLEXITY_API_KEY= 

# OpenAI engine/model to use (chat-capable models like gpt-3.5-turbo recommended)
OPENAI_ENGINE=gpt-3.5-turbo

# If you don't use a remote provider, set HF_MODEL to use a local transformers model (e.g., gpt2)
HF_MODEL=gpt2

# Optional other providers (best-effort wrappers):
# GEMINI_API_KEY= (Google Gemini / PaLM style key)
GEMINI_API_KEY=
# DEEPSEEK_API_KEY= (Deepseek provider key, if you have one)
